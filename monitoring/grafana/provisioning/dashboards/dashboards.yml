apiVersion: 1

providers:
  - name: "vllm-controlplane"
    orgId: 1
    folder: "vLLM Control Plane"
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
